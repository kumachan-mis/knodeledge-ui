import ProjectLayout from '@/components/organisms/projects/ProjectLayout';

const Layout: React.FC<{ children: React.ReactNode }> = ({ children }) => <ProjectLayout>{children}</ProjectLayout>;

export default Layout;
