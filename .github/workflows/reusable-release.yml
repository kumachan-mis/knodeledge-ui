name: Release

on:
  workflow_call:

jobs:
  release:
    steps:
      - name: Release new version
        run: yarn semantic-release
    permissions:
      contents: write
      issues: write
      pull-requests: write
      id-token: write
    env:
      GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
